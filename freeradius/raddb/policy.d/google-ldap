# Normalize login to Google uid (local part only).
# Works with juan, juan@cedrosnorte.edu.mx, or juan@colegios-cedros-paseo.mx.
google_ldap_strip_username {
	if (&User-Name =~ /^([^@]+)@/) {
		update request {
			&User-Name := "%{tolower:%{1}}"
		}
	}
}
