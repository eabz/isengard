# Multi-domain Google Workspace (e.g. cedrosnorte.edu.mx + colegios-cedros-paseo.mx):
# - base_dn uses the PRIMARY domain only
# - WiFi clients must send the full email as User-Name / Identity
# - Do not strip @domain; LDAP matches mail= user@any-workspace-domain
google_ldap_strip_username {
}
